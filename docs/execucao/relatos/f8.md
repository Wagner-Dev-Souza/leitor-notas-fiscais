Dono: **gula**. Spec: `docs/execucao/spec-F8.md`. Interface: secoes 3.2 e 3.3 de `docs/execucao/00b-contrato-fechamento.md`.

Entrega: `app/canais.py` (novo) - `TransporteHTTP` injetavel, `transporte_urllib()`,
`coletar_telegram()` (getUpdates), `ler_webhook_whatsapp()`, `coletar()`, `ErroCanal`,
`ResultadoColeta` - e `tools/receber_webhook_whatsapp.py` (receptor local com verificacao por
`hub.verify_token`).

Os envelopes gravados tem de ser digeriveis pelo `app/ingress.py` atual, sem tocar no pipeline.
Nenhuma chamada a `api.telegram.org`/`graph.facebook.com`: prova com stub HTTP local em 127.0.0.1.

Evidencia em `docs/execucao/_ids/relato-F8.md`: coleta contra stub, GET/POST no receptor com o
arquivo gravado, pipeline rodando sobre os envelopes coletados, `pytest -q`. Nao rodar git.
