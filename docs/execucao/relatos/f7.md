Dono: **avareza**. Spec: `docs/execucao/spec-F7.md`. Interface: secao 3.1 e 3.6 de `docs/execucao/00b-contrato-fechamento.md`.

Entrega: `app/config.py` (novo) - parser proprio do `.env` (so stdlib), catalogo de variaveis
(`VARIAVEIS`), `ConfigError` citando pelo nome exatamente o que falta, `mascarar()`,
`imprimir_configuracao()`, `exemplo_env()` - e `app/run.py` ajustado (`--real`, `--env`,
`--check-config`, log em arquivo, mensagem clara de configuracao faltante com codigo 2).

O modo mock continua padrao e a saida de terminal da fase 2 nao pode regredir.

Evidencia em `docs/execucao/_ids/relato-F7.md`: `--mock`, `--real` sem `.env`, `--check-config`
com `.env` temporario (segredo mascarado) e `pytest -q`. Nao rodar git.
