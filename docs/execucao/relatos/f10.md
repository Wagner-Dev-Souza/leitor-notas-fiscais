Dono: **ira**. Spec: `docs/execucao/spec-F10.md`. Precisa de F7 e F8 no worktree.

Entrega: `tests/test_config.py`, `tests/test_canais.py`, `tests/test_producao.py` cobrindo
catalogo de variaveis, mensagem de falta por nome, modo padrao mock, modo real ponta a ponta com
coleta sem credencial real (stub local), higiene de segredo (`git ls-files` sem `.env`, varredura
de segredo versionado, `.env.example` sem valor plausivel) e `--check-config` mascarando segredo.

Evidencia em `docs/execucao/_ids/relato-F10.md`: `pytest -q` (284 + novos), `pytest -v` dos novos,
saida salva em `tests/evidencia/`, e o que NAO foi testado contra servico real. Nao rodar git.
