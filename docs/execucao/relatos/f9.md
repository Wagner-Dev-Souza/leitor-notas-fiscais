Dono: **preguica**. Spec: `docs/execucao/spec-F9.md`. Precisa de F7 (`app/config.py`) no worktree.

Entrega: `tools/gerar_env_example.py` (gera/conferE o `.env.example` a partir de
`app.config.VARIAVEIS`), `.env.example` **versionado na raiz** com todas as variaveis, valores
placeholders/vazios e comentario por variavel (o que e, obrigatoria em que modo, onde obter) e
`tools/verificar_producao.py` (6 checks com execucao real, PASSOU/FALHOU, sai != 0 se falhar,
sem escrever em `data/`).

Evidencia em `docs/execucao/_ids/relato-F9.md`: `gerar_env_example.py`, `--conferir`,
`verificar_producao.py` e `pytest -q`. Nao rodar git.
