# Dependencias do pipeline de leitura de notas fiscais/pedidos (squad-pecados).
# Versoes = exatamente as instaladas no .venv do projeto (CPython 3.12.14).
#
# Instalar:
#   uv pip install --python .venv/Scripts/python.exe -r requirements.txt
#
# Nao crie outro venv: o interpretador do projeto e `.venv/Scripts/python.exe`.

pypdf==6.19.0
pdfplumber==0.11.10
reportlab==5.0.1
openpyxl==3.1.5
pytest==9.1.1
pillow==12.3.0

# --- OCR real (Tesseract) ----------------------------------------------------
# O motor real esta INSTALADO e em uso: binario Tesseract 5.5.3 (instalador oficial,
# com o idioma `por`) + o pacote `pytesseract` abaixo. E ele que le a imagem (foto/
# print de nota) e o PDF escaneado.
#
# Ordem de leitura do projeto: tesseract real -> sidecar `.ocr.txt` (simulado, so
# existe no material sintetico) -> texto vazio marcado para revisao humana. Sem o
# binario, o caminho simulado assume e sai ROTULADO como simulado na trilha.
#
# O binario NAO vem pelo pip. No Windows:
#   winget install --id tesseract-ocr.tesseract -e
# `app/ingress.localizar_tesseract()` encontra o executavel pelo PATH, por
# `TESSERACT_CMD` ou no local padrao de instalacao.
pytesseract==0.3.13
